source "https://rubygems.org"

# Mirrors the exact versions GitHub Pages builds with, so a local preview
# matches what gets deployed. Only needed for local previews — GitHub does
# not read this file when building the site.
gem "github-pages", group: :jekyll_plugins

gem "webrick", "~> 1.8"   # no longer bundled with Ruby 3+
